parents:
    - config

subnet:
    nat-a:
        when_in: "@aws_nat_subnets"
        network: "@aws_networks"
        cidr_prefix: 28
        zone_suffix: a
        use_public_ip: true
        use_nat: true
        groups: "@network_groups"

    nat-b:
        when_in: "@aws_nat_subnets"
        network: "@aws_networks"
        cidr_prefix: 28
        zone_suffix: b
        use_public_ip: true
        use_nat: true
        groups: "@network_groups"

    nat-c:
        when_in: "@aws_nat_subnets"
        network: "@aws_networks"
        cidr_prefix: 28
        zone_suffix: c
        use_public_ip: true
        use_nat: true
        groups: "@network_groups"
